// Generated by gen:page — mode: dynamic
// Route: {{routePath}}
// This page contains async Server Components.
// Data fetching runs on the server — no 'use client' needed unless adding interactivity.
// TODO: Replace placeholder data fetching with real implementation.

import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '{{Name}}',
}

// TODO: Replace with real async server-side data fetching.
async function fetch{{Name}}Data() {
  // Example: const data = await db.query(...)
  return {}
}

export default async function {{Name}}Page() {
  const data = await fetch{{Name}}Data()

  return (
    <main className="mx-auto max-w-2xl space-y-6 p-8">
      <header>
        <h1 className="text-2xl font-bold text-zinc-50">{{Name}}</h1>
        {/* TODO: Add subtitle or breadcrumb */}
      </header>

      {/* TODO: Implement {{Name}} page content using data */}
    </main>
  )
}
