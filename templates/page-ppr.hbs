// Generated by gen:page — mode: ppr
// Route: {{routePath}}
// Shell-first / streaming structure using DynamicSection.
// PPR is enabled via cacheComponents: true in next.config.ts — no per-page flag needed.
// TODO: Replace ExampleAsyncSection with real async Server Components.

import type { Metadata } from 'next'
import { DynamicSection } from '@vibe/ui'

export const metadata: Metadata = {
  title: '{{Name}}',
}

// TODO: Replace this placeholder with a real async Server Component.
// It may perform DB queries, fetch calls, or any other async work.
async function ExampleAsyncSection() {
  // await new Promise<void>((resolve) => setTimeout(resolve, 200))
  return (
    <div className="rounded-md border border-zinc-700 bg-zinc-900 px-4 py-3 text-sm text-zinc-300">
      {/* TODO: Implement async content for {{Name}} */}
      ExampleAsyncSection placeholder
    </div>
  )
}

export default function {{Name}}Page() {
  return (
    <main className="mx-auto max-w-2xl space-y-6 p-8">
      {/* Static shell — rendered immediately */}
      <header>
        <h1 className="text-2xl font-bold text-zinc-50">{{Name}}</h1>
        {/* TODO: Add subtitle or breadcrumb */}
      </header>

      {/* Streaming hole — streams in after the shell is sent */}
      <DynamicSection>
        <ExampleAsyncSection />
      </DynamicSection>
    </main>
  )
}
