# @vibe/config

Phase 6+ placeholder. This package will contain shared tooling
configuration (ESLint presets, TypeScript base configs, etc.).

Currently empty by design. Do not add content until Phase 6 is active.

See `openspec/project.md` → Phase Boundaries for details.
